enum Aldersgruppe {
        JUNIOR,
        SENIOR
    }

enum Disciplin {
    SINGLE,
    DOUBLE,
    MIXED_DOUBLE
}

enum SpillerNiveau {
    MOTIONIST,
    KONKURRENCE
}

enum Medlemstype {
    AKTIV,
    PASSIV
}

enum BetalingsStatus {
    BETALT,
    IKKE_BETALT,
    RESTANCE
}
